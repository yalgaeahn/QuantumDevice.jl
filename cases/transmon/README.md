# Transmon Case

This workspace uses the upstream `DeviceLayout.jl` single-transmon example as the first reference case.

The live source file is staged into `build/transmon/work/SingleTransmon.jl` by `scripts/build_transmon.jl` so all generated mesh, GDS, and Palace input files stay inside this workspace.
